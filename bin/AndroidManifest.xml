<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bg.androidcourse.cookingplanner"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/cooking"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="bg.androidcourse.cookingplanner.activities.StartActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustPan"
            android:configChanges="screenLayout|screenSize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="bg.androidcourse.cookingplanner.activities.SearchActivity"
            android:label="@string/app_name"
            android:configChanges="screenLayout|screenSize" >
        </activity>
        <activity
            android:name="bg.androidcourse.cookingplanner.activities.RecipeActivity"
            android:label="@string/app_name"
            android:configChanges="screenLayout|screenSize" >
        </activity>
        <activity
            android:name="bg.androidcourse.cookingplanner.activities.ResultsActivity"
            android:label="@string/app_name"
            android:configChanges="screenLayout|screenSize" >
        </activity>
    </application>

</manifest>
